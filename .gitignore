node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
# The worker's credential file is ignored by the line above; its EXAMPLE is not
# a secret and has to survive, or a fresh host has no way to know what to fill in.
!.env.tracking-worker.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.DS_Store
.idea
.vscode

# Prepared import payloads: derived from an operator's own export, regenerated by
# scripts/prepare-*.ts. The generator is the reusable thing; a single tenant's
# catalogue is not repository data.
scripts/*.prepared.json
